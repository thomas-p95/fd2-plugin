---
title: Specs
description: Accept requirements, execute full feature workflow (branch through review)
---

# Specs


Accept **requirements** (or a clear task) from the user, then execute the feature workflow defined in `${CLAUDE_PLUGIN_ROOT}/rules/development-workflow.md`. Do not require or assume a ticket or issue ID.

**Strictly follow steps 1 → 4 in order.** Each step must be fully completed before moving to the next. Do not skip or reorder any step.
